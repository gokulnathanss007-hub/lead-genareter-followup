import { defineConfig } from "@trigger.dev/sdk";

export default defineConfig({
  // Get your project ref from: cloud.trigger.dev → your project → Settings
  project: "proj_atuowbgazsctjnltnapi",
  dirs: ["./src/trigger"],
  maxDuration: 3600,
});
